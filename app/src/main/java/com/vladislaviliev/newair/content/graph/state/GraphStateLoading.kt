package com.vladislaviliev.newair.content.graph.state

import com.vladislaviliev.newair.content.ScreenStateConstants

object GraphStateLoading {
    val value = GraphState(emptyList(), ScreenStateConstants.LOADING)
}